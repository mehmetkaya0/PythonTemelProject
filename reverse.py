def reverseFunction(aList):
    result = []
    for i in range(len(aList))
        for j in range(len(aList[i]))
            aList[i] = aList[i].reverse()
            aList[i].insert(i,result)
    return result

